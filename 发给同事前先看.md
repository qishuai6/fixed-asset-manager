# 固资管理系统交付说明

这是一个可本地运行的轻量固资管理系统，适合 `1-2` 位管理员在单台电脑上使用。

## 发给别人时建议直接发这两个文件

1. `fixed-asset-manager-delivery.zip`
2. 如果对方不会装环境，再单独告诉他：
   `需要先安装 Node.js 20 或更高版本`

## 对方拿到后怎么用

### macOS

1. 解压 `fixed-asset-manager-delivery.zip`
2. 双击 `启动固资系统.command`
3. 第一次运行会自动安装依赖，时间会久一点
4. 浏览器会自动打开 `http://127.0.0.1:8899`

### Windows

1. 解压 `fixed-asset-manager-delivery.zip`
2. 双击 `启动固资系统.bat`
3. 第一次运行会自动安装依赖，时间会久一点
4. 浏览器会自动打开 `http://127.0.0.1:8899`

## 默认数据位置

- 数据库文件在 `data/fixed-assets.sqlite`
- 资产、人员、办理记录都存在本地电脑里

## 导入模板

- `public/templates/assets-template.csv`
- `public/templates/employees-template.csv`

## 重要说明

- 这是本地单机版，不是多人同时在线版
- 一台电脑上的数据不会自动同步到另一台电脑
- 如果要迁移数据，把 `data/fixed-assets.sqlite` 一起拷走即可
